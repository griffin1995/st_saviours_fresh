// CMS DATA SOURCE: Prayer Hub components export index
// Following project patterns for component organization and TypeScript exports

// Prayer component types - re-export CMS prayer types for convenience
export type {
  Prayer,
  PrayerCategory,
  PrayerRequestCategory,
  PrayerIntention
} from '@/lib/cms/cms-content';
